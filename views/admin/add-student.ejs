<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Student - Certify</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <!-- Top Navigation -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <span class="text-2xl font-bold text-indigo-600">Certify</span>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-700">Welcome, <%= user ? user.name : 'Admin' %></span>
                    <a href="/auth/logout" class="text-red-600 hover:text-red-800">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="flex">
        <!-- Sidebar -->
        <div class="w-64 bg-white shadow-lg h-screen">
            <div class="p-4">
                <nav class="space-y-2">
                    <a href="/admin/dashboard" class="flex items-center space-x-2 p-2 text-gray-600 hover:bg-gray-50 rounded-lg">
                        <i class="fas fa-home"></i>
                        <span>Dashboard</span>
                    </a>
                    <a href="/admin/subjects" class="flex items-center space-x-2 p-2 text-gray-600 hover:bg-gray-50 rounded-lg">
                        <i class="fas fa-book"></i>
                        <span>Subjects</span>
                    </a>
                    <a href="/admin/certificates" class="flex items-center space-x-2 p-2 text-gray-600 hover:bg-gray-50 rounded-lg">
                        <i class="fas fa-certificate"></i>
                        <span>Certificates</span>
                    </a>
                    <a href="/admin/users" class="flex items-center space-x-2 p-2 text-gray-600 hover:bg-gray-50 rounded-lg">
                        <i class="fas fa-users"></i>
                        <span>Users</span>
                    </a>
                    <a href="/admin/verifications" class="flex items-center space-x-2 p-2 text-gray-600 hover:bg-gray-50 rounded-lg">
                        <i class="fas fa-check-circle"></i>
                        <span>Verifications</span>
                    </a>
                </nav>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 p-8">
            <div class="max-w-3xl mx-auto">
                <h1 class="text-2xl font-bold text-gray-800 mb-6">Add New Student</h1>
                
                <% if (message) { %>
                    <div class="bg-<%= message.includes('success') ? 'green' : 'red' %>-100 border border-<%= message.includes('success') ? 'green' : 'red' %>-400 text-<%= message.includes('success') ? 'green' : 'red' %>-700 px-4 py-3 rounded mb-4">
                        <%= message %>
                    </div>
                <% } %>
                
                <form action="/admin/students" method="POST" class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
                    <div class="mb-4">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="name">
                            Full Name
                        </label>
                        <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                               id="name" 
                               name="name" 
                               type="text" 
                               placeholder="Student's full name"
                               required>
                    </div>
                    
                    <div class="mb-4">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="email">
                            Email
                        </label>
                        <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                               id="email" 
                               name="email" 
                               type="email" 
                               placeholder="student@example.com"
                               required>
                    </div>
                    
                    <div class="mb-4">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="registrationNumber">
                            Registration Number
                        </label>
                        <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                               id="registrationNumber" 
                               name="registrationNumber" 
                               type="text" 
                               placeholder="Student registration number"
                               required>
                        <p class="text-sm text-gray-500 mt-1">
                            <i class="fas fa-info-circle mr-1"></i> 
                            This is the unique registration number assigned to the student.
                        </p>
                    </div>
                    
                    <div class="mb-4">
                        <label class="block text-gray-700 text-sm font-bold mb-2" for="blockchainAddress">
                            Blockchain Address
                        </label>
                        <div class="flex space-x-2">
                            <input class="shadow appearance-none border rounded flex-1 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                                   id="blockchainAddress" 
                                   name="blockchainAddress" 
                                   type="text" 
                                   placeholder="0x..."
                                   required>
                            <button type="button" 
                                    onclick="generateBlockchainAddress()"
                                    class="bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
                                Generate
                            </button>
                        </div>
                        <p class="text-sm text-gray-500 mt-1">
                            <i class="fas fa-info-circle mr-1"></i> 
                            This address will be used to store critical student information on the blockchain.
                        </p>
                    </div>
                    
                    <div class="mb-6">
                        <label class="block text-gray-700 text-sm font-bold mb-2">
                            Subjects
                        </label>
                        <div class="border rounded p-4 max-h-60 overflow-y-auto">
                            <% if (subjects && subjects.length > 0) { %>
                                <% subjects.forEach(subject => { %>
                                    <div class="flex items-center mb-2">
                                        <input type="checkbox" 
                                               id="subject_<%= subject._id %>" 
                                               name="subjects[]" 
                                               value="<%= subject._id %>"
                                               class="mr-2">
                                        <label for="subject_<%= subject._id %>">
                                            <%= subject.code %> - <%= subject.name %> (<%= subject.credits %> credits)
                                        </label>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <p class="text-gray-500">No subjects available. Please add subjects first.</p>
                            <% } %>
                        </div>
                    </div>
                    
                    <div class="flex items-center justify-between">
                        <div class="flex space-x-4">
                            <button class="bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" 
                                    type="submit">
                                Add Student
                            </button>
                            <button type="button" 
                                    onclick="window.history.back()"
                                    class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
                                <i class="fas fa-arrow-left mr-2"></i> Go Back
                            </button>
                        </div>
                        <a href="/admin/students" class="text-indigo-500 hover:text-indigo-800">
                            Cancel
                        </a>
                    </div>
                </form>
                
                <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                    <h3 class="text-lg font-semibold text-blue-800 mb-2">
                        <i class="fas fa-database mr-2"></i> Data Storage Information
                    </h3>
                    <p class="text-blue-700 mb-2">
                        Student information is stored in two places:
                    </p>
                    <ul class="list-disc list-inside text-blue-700 space-y-1">
                        <li><strong>MongoDB:</strong> All student details including personal information, subjects, and credentials.</li>
                        <li><strong>Blockchain:</strong> Critical student information including name, email, blockchain address, and enrolled subjects for verification purposes.</li>
                    </ul>
                    <p class="text-blue-700 mt-2">
                        This dual storage approach ensures data integrity and allows for blockchain-based verification of student credentials.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Function to generate a random Ethereum address
        function generateRandomAddress() {
            // Generate a random 40-character hex string (20 bytes)
            const randomBytes = Array.from({length: 40}, () => 
                Math.floor(Math.random() * 16).toString(16)
            ).join('');
            
            // Format as Ethereum address
            return '0x' + randomBytes;
        }
        
        // Set a random address when the page loads
        window.onload = function() {
            document.getElementById('blockchainAddress').value = generateRandomAddress();
        };
        
        // Function to generate a new address when button is clicked
        function generateBlockchainAddress() {
            document.getElementById('blockchainAddress').value = generateRandomAddress();
        }
    </script>
</body>
</html> 