<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>General Settings - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .settings-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .settings-header {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            border-radius: 15px 15px 0 0;
            padding: 20px;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .btn-save {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
        }
        .btn-save:hover {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="settings-card">
                    <div class="settings-header">
                        <h4 class="mb-0">
                            <i class="fas fa-cog me-2"></i>
                            General Settings
                        </h4>
                    </div>
                    <div class="card-body p-4">
                        <% if (error) { %>
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <%= error %>
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        <% } %>
                        
                        <form id="settingsForm">
                            <div class="mb-3">
                                <label for="instituteName" class="form-label">Institute Name</label>
                                <input type="text" class="form-control" id="instituteName" name="instituteName" 
                                    value="<%= settings.instituteName %>" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="instituteEmail" class="form-label">Institute Email</label>
                                <input type="email" class="form-control" id="instituteEmail" name="instituteEmail" 
                                    value="<%= settings.instituteEmail %>" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="maxStudents" class="form-label">Maximum Number of Students</label>
                                <input type="number" class="form-control" id="maxStudents" name="maxStudents" 
                                    value="<%= settings.maxStudents %>" min="1" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="certificateExpiry" class="form-label">Certificate Expiry (days)</label>
                                <input type="number" class="form-control" id="certificateExpiry" name="certificateExpiry" 
                                    value="<%= settings.certificateExpiry %>" min="1" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="blockchainNetwork" class="form-label">Blockchain Network</label>
                                <select class="form-select" id="blockchainNetwork" name="blockchainNetwork" required>
                                    <option value="Ganache" <%= settings.blockchainNetwork === 'Ganache' ? 'selected' : '' %>>Ganache</option>
                                    <option value="Testnet" <%= settings.blockchainNetwork === 'Testnet' ? 'selected' : '' %>>Testnet</option>
                                    <option value="Mainnet" <%= settings.blockchainNetwork === 'Mainnet' ? 'selected' : '' %>>Mainnet</option>
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="web3Provider" class="form-label">Web3 Provider URL</label>
                                <input type="text" class="form-control" id="web3Provider" name="web3Provider" 
                                    value="<%= settings.web3Provider %>" required>
                                <div class="form-text">
                                    <i class="fas fa-info-circle me-1"></i>
                                    Default: http://127.0.0.1:7545 (Ganache)
                                </div>
                            </div>
                            
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-save">
                                    <i class="fas fa-save me-2"></i>
                                    Save Settings
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('settingsForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());
            
            try {
                const response = await fetch('/admin/settings/general', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(data)
                });
                
                const result = await response.json();
                
                if (result.success) {
                    const alertDiv = document.createElement('div');
                    alertDiv.className = 'alert alert-success alert-dismissible fade show';
                    alertDiv.innerHTML = `
                        <strong>Success!</strong> ${result.message}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    `;
                    document.querySelector('.card-body').prepend(alertDiv);
                } else {
                    const alertDiv = document.createElement('div');
                    alertDiv.className = 'alert alert-danger alert-dismissible fade show';
                    alertDiv.innerHTML = `
                        <strong>Error!</strong> ${result.message}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    `;
                    document.querySelector('.card-body').prepend(alertDiv);
                }
            } catch (error) {
                console.error('Error:', error);
                const alertDiv = document.createElement('div');
                alertDiv.className = 'alert alert-danger alert-dismissible fade show';
                alertDiv.innerHTML = `
                    <strong>Error!</strong> Failed to update settings. Please try again.
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                `;
                document.querySelector('.card-body').prepend(alertDiv);
            }
        });
    </script>
</body>
</html> 