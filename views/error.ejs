<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Certify</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="max-w-md w-full bg-white shadow-lg rounded-lg overflow-hidden">
        <div class="bg-red-500 px-6 py-4">
            <h1 class="text-white text-xl font-bold">Error</h1>
        </div>
        <div class="p-6">
            <div class="mb-4">
                <p class="text-gray-700 text-lg mb-2"><%= message %></p>
                <% if (error) { %>
                    <p class="text-gray-500 text-sm"><%= error %></p>
                <% } %>
            </div>
            <div class="flex justify-between">
                <a href="javascript:history.back()" class="text-indigo-500 hover:text-indigo-700">
                    <i class="fas fa-arrow-left mr-2"></i> Go Back
                </a>
                <a href="/" class="text-indigo-500 hover:text-indigo-700">
                    <i class="fas fa-home mr-2"></i> Home
                </a>
            </div>
        </div>
    </div>
</body>
</html> 